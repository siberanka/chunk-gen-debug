# Security policy

Please report vulnerabilities privately through GitHub Security Advisories.
Do not attach production logs: diagnostic records can contain world UUIDs,
plugin names, class names, coordinates, and—if explicitly enabled—player IDs.

The default configuration excludes player identities. Review and redact logs
before sharing them.
